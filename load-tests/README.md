# Load Tests

k6 scripts used to generate real traffic and measure real numbers (never invented — see project rules).

Planned: `normal-load.js`, `stress-test.js`, `failure-test.js`. Built in Phase 15.
